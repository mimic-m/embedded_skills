# [ドキュメント修正] README: change-spec スキル名参照の修正とコードブロックフォーマット対応

> **作成日:** 2026-05-02
> **作成モード:** 実装後
> **参照:** コミット b877260

---

## 要件

**What（変更対象）:**
`README.md` 内の以下4箇所：

1. スキル一覧テーブルの `change-spec-from-diff` 行（名前・リンクパス・説明文）
2. インストールスクリプトのスキル名リスト
3. 使用方法セクションの `change-spec-from-diff` ヘッダおよび説明文
4. 全6箇所のコードブロック（言語指定なし）

**Why（変更理由）:**
`change-spec-from-diff` スキルが `change-spec` にリネームされたにもかかわらず README の参照が旧名のままで、インストールスクリプトが存在しないパスを参照していた。また、コードブロックに言語指定がなく markdownlint 警告（MD040）が発生していた。

**What → What（変更前後）:**

| 項目 | 変更前 | 変更後 |
| --- | ------ | ------ |
| テーブル行のスキル名 | `change-spec-from-diff` | `change-spec` |
| テーブル行のリンクパス | `skills/change-spec-from-diff/SKILL.md` | `skills/change-spec/SKILL.md` |
| テーブル行の説明文 | 「git diff から変更仕様書（Markdown）を生成する」 | 「git diff または issue・要件テキストから変更仕様書（Markdown）を生成する」 |
| インストールスクリプトのスキル名 | `change-spec-from-diff` | `change-spec` |
| 使用方法セクションのヘッダ | `### \`change-spec-from-diff\`` | `### \`change-spec\`` |
| 使用方法の説明文 | 「関数追加・インターフェース変更…Markdownを出力」 | issue/要件テキスト/git diffを入力として受け付ける旨を追記 |
| コードブロック言語指定 | ` ``` `（指定なし）× 6箇所 | ` ```text ` × 6箇所 |

---

## 変更概要

`change-spec` スキルへのリネームに追従して README の参照名・リンクパス・説明文を更新した。あわせて既存の全コードブロックに `text` 言語指定を追加し markdownlint 警告を解消した。

> ✅ **破壊的変更なし**（ドキュメントのみ、スキル本体・インストール済み環境への影響なし）

---

## 変更詳細

### シーケンス図

該当なし（ドキュメント変更のみ）

### 追加/修正するファイル

| ファイル | 変更種別 | 変更内容 |
| -------- | -------- | -------- |
| `README.md` | 修正（確定） | スキル名・リンクパス・説明文の更新、コードブロック言語指定の追加 |
