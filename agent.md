# Agent Guidelines

## 基本方針

このリポジトリで作業するエージェントは、Spec-Driven Development (SDD) を基本とする。実装を急がず、先に目的、仕様、受け入れ条件、検証方法を明確にする。

作業では以下を優先する。

- 既存の設計、文体、ディレクトリ構成に合わせる
- 変更範囲を依頼目的に絞る
- 仕様と実装の対応を説明できる状態にする
- 検証結果を確認してから完了を報告する
- 文章は高密度にし、不要な前置き、謝辞、曖昧なヘッジを減らす

## 作業開始時のGrill-Me

作業開始時は、grill-me の要素を取り入れる。

- 不明点、前提、分岐する設計判断を洗い出す
- 質問は一度に詰め込みすぎず、依存関係のある判断から順に確認する
- 各質問には推奨回答を添える
- コードベースや既存ドキュメントから答えられることは、ユーザーへ質問する前に調べる
- ユーザーが `ok` などで承認した内容は、以後の仕様前提として扱う

ただし、明らかに小さい修正や既存仕様から判断できる作業では、必要最小限の確認に留めて進める。

## 圧縮コミュニケーション

通常のやり取りは、caveman lite 相当の圧縮率を目安にする。ただし、原始人ロール、片言、過度な断片化は使わない。思考の幅を狭めず、技術的な正確さと判断理由を保つ。

方針:

- 冗長な前置き、社交辞令、過剰な謝辞を省く
- 「たぶん」「おそらく」などの曖昧な表現は、根拠や不確実性を具体化する
- 完全文を基本にしつつ、短い文で要点を先に出す
- 技術用語、コマンド、エラー文、ファイル名は省略しない
- 複雑な手順、危険な操作、セキュリティ注意、不可逆操作では圧縮しすぎない
- ユーザーが詳細説明を求めた場合は、圧縮より明確さを優先する

推奨パターン:

```text
対象。判断。理由。次の操作。
```

例:

```text
`agent.md` を更新します。目的はSDD開始時の確認品質を上げることです。変更後に `git diff --check` で検証します。
```

## SDD Workflow

標準フローは以下とする。

1. 依頼内容を読む
2. 既存コード、既存スキル、関連ドキュメントを確認する
3. grill-me形式で不明点を解消する
4. 仕様と受け入れ条件を短く確定する
5. 実装または文書更新の計画を立てる
6. 変更を行う
7. 仕様に対応する検証を実行する
8. 変更内容、検証結果、残課題を報告する

仕様には最低限、以下を含める。

- 対象ファイルまたは対象機能
- 入力
- 出力
- エラー条件
- 変更しない範囲
- 検証方法

## 実装ルール

- 既存ファイルを読む前に大きな変更をしない
- 関連しないファイルを変更しない
- 既存の未コミット変更はユーザー作業として扱い、勝手に戻さない
- 手作業の編集は最小単位で行う
- サンプル、テンプレート、仕様書を追加する場合は、参照元から辿れるようにする

## 検証ルール

完了報告の前に、変更内容に合う検証を実行する。

- Markdownやスキル文書の変更: `git diff --check`
- スクリプト変更: 構文チェックと最小実行
- テスト生成ルール変更: サンプル差分で生成・実行を確認
- 外部ツール連携仕様: 実行可否、必要な前提、未実装範囲を明記

検証できなかった項目は、理由と残リスクを報告する。

## 報告ルール

報告では以下を簡潔に示す。

- 変更した内容
- 変更したファイル
- 実行した検証
- 検証結果
- 残課題または次の推奨アクション

完了、成功、修正済みと述べる前に、必ず検証結果を確認する。
